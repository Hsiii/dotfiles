" Show absolute line numbers in the left gutter.
set number

" Enable mouse support for cursor movement, selection, scrolling, and splits.
set mouse=a

" Highlight the current cursor line for easier navigation.
set cursorline

" Briefly show matching brackets when typing a closing bracket.
set showmatch

" Keep the sign column stable for Git and diagnostics markers.
set signcolumn=yes

" Always show the status line for editor and file context.
set laststatus=2
set statusline=%f\ %m%r%h%w%=%y\ %{&fileencoding?&fileencoding:&encoding}\ [%l:%c]\ %p%%

" Improve command-line completion for files, buffers, commands, and options.
set wildmenu
set wildmode=longest:full,full

" Show completion menus without preselecting an item.
set completeopt=menuone,noselect

" Open new splits in the same direction as modern editor panes.
set splitright
set splitbelow

" Allow switching away from modified buffers without saving first.
set hidden

" Reload files changed outside Vim when it is safe to do so.
set autoread

" Refresh async editor feedback quickly, including GitGutter signs.
set updatetime=100

" Use the system clipboard for yank, delete, change, and paste operations.
set clipboard=unnamedplus

" Keep undo history across Vim sessions outside the dotfiles repository.
set undofile
set undodir=~/.local/state/vim/undo

" Keep backup and swap files out of project directories.
set backupdir=~/.local/state/vim/backup//
set directory=~/.local/state/vim/swap//

" Make search interactive, highlighted, and smart about casing.
set ignorecase
set smartcase
set incsearch
set hlsearch

" Wrap long visual lines at word boundaries and preserve indentation.
set linebreak
set breakindent

" Use real tab characters and display each tab as four columns.
set noexpandtab
set tabstop=4
set shiftwidth=4
set softtabstop=0

" Continue the previous line's indentation on new lines.
set autoindent

" Load filetype-specific plugins and indentation rules.
filetype plugin indent on

" Use vim-move for VS Code-style line and selection movement.
let g:move_map_keys = 0
nmap <A-Down> <Plug>MoveLineDown
nmap <A-Up> <Plug>MoveLineUp
xmap <A-Down> <Plug>MoveBlockDown
xmap <A-Up> <Plug>MoveBlockUp

" Save files with the common editor shortcut.
nnoremap <C-s> :write<CR>
inoremap <C-s> <Esc>:write<CR>gi
xnoremap <C-s> <Esc>:write<CR>gv

" Enable syntax highlighting and true-color terminal themes.
syntax on
set termguicolors
colorscheme rosepine_moon
